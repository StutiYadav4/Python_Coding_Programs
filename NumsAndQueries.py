nums = eval(input("Enter the nums list :"))

queries = eval(input("Enter the queries list :"))

lst = []
for ind in queries:
    for j in ind:
        trimIndex = j[1]
        print(trimIndex)
        for i in nums:
            trim i such that only trimIndex digits remain




