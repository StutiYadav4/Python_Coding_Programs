'''def solve_coin_change(denomination, amount):
    solution = 0
    for i in denomination:
        rem = amount % i
        if rem == 0:
            solution += 1
    if n * ind + m * j == amount:
        solution += 1
    print(solution)
# So this method is not really fruitful or feasible as it is not really giving any result.'''

def solve_coin_change(denomination, amount):
    if




denomination = [1, 2, 5]
amount = 5
solve_coin_change(denomination, amount)
